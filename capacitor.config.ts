import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'formulario-de-registro',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
